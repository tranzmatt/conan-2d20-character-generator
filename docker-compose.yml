services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: conan2d20-character-generator:latest
    container_name: conan2d20-character-generator
    ports:
      - "8080:80"
    restart: unless-stopped
