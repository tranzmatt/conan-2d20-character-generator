import * as React from 'react';

export const PDFNotice = () => (
  <div className="pdf-notice">
    <strong>PDF Export:</strong> Use "Export PDF (Template)" to download with the Modiphius character sheet layout, or
    "Export PDF (Print)" for browser print-to-PDF.
  </div>
);
