import * as React from 'react';

export const PDFNotice = () => (
  <div className="pdf-notice">
    <strong>Note:</strong> Online PDF export is offline. Use the local "Export PDF (Local)" button on the finish
    page.
  </div>
);
